# 电商选品MCP - 项目交接文档

> 给接手的AI：读完这份文档你就能继续干活。这是一个**已经跑通核心闭环**的真实项目，
> 不是从零开始。先理解现状，别推倒重来。

## 一、这是什么

一个**电商选品自动化工具**，做成 MCP（让任何 AI Agent 都能调用）。

**业务逻辑（无货源套利）**：
1. 在**京东**找"买手店"的商品（买手店 = 非旗舰店/非官方店/非海外店的普通店铺）
2. 拿京东品名去**淘宝**找同款货源
3. 比价算利润，筛出"国内发货 + 48小时内发货 + 已售>10 + 利润合规"的品
4. 存数据库，导出 CSV 表格

**商业模式**：引擎通用 + 策略可配。核心引擎开源吸引用户，
策略库/服务器/多账号托管作为收费 SaaS。用户给自己的 Agent 装这个 MCP，按用量计费。
所以**策略不能写死**，必须可配置（已实现 strategy-engine.js）。

## 二、当前进度（重要！别重复造轮子）

**已经跑通并验证的完整闭环**（用京东账号二 + 淘宝账号一真实跑过）：
```
京东选品(买手店,带评价数+截图) → 淘宝比价(国内+48h+已售≥10,带SKU+实付价+截图)
  → 存库 → 导出CSV
```

**核心技术难题都已解决**：
- 京东/淘宝搜索：模拟人类操作（鼠标移动→点击→打字→点搜索），绕过反爬
- 搜索框选择器：京东是 `input.jd_pc_search_bar_react_search_input`（2026新版React）
- 商品提取：不依赖动态class，用 `data-sku` + innerText 智能解析
- 进详情页：模拟点击图片（京东用JS跳转，不是直接URL）
- 淘宝发货地：认快递行"广东汕头 至 邢台"格式，**区分"产地澳洲"≠"发货地广东"**

## 最近更新（feat/price-comparison-v2，2026-06-10）

### 策略引擎已接入harvest
`loadStrategyDefaults()` 从策略引擎读默认值，jd_harvest 和 taobao_harvest 的筛选条件不再硬编码。
改 `lib/strategy-engine.js` 里的参数就能影响实际行为。调用方仍可传参覆盖。

| 参数 | 来源 | 默认值 |
|---|---|---|
| JD 最低评价 | strategy.jd.minComments | 2 |
| JD 价格区间 | strategy.jd.priceRange | [80, 999999] |
| 淘宝 最低已售 | strategy.taobao.minSales | 10 |
| 淘宝 国内/48h | strategy.taobao.shipFrom / shipWithinHours | domestic / 48 |
| 淘宝 价格区间 | strategy.taobao.priceRange | [80, 999999] |

### 浏览器永不关闭
`chrome.js`/`accounts.js`/`sourcing.js` 全部去掉了杀浏览器进程的逻辑。
浏览器打开后不会关闭——避免反复开闭触发风控。`close` action 改为空操作。

### 淘宝线全通
搜→列表筛(国内+48h+已售+价格)→进详情(SKU+实付价+截图)→翻页→串联→入库导出。每步单独验证。
- 标题用 `ItemHead` / `document.title` 兜底
- 店铺名用 `[class*="shopName"]` 只取第一行
- 翻页按钮: `BUTTON.next-btn` + 文本"下一页"
- 详情价格抓 `highlightPrice`(实付价)，不抓 `subPrice`(划线价)

### CSV 导出加截图列
`export_results` 导出的CSV增加了"京东截图"和"淘宝截图"两列（存本地路径）。

### 搜索用JS原生事件
`aiJdSearch` 改用 `page.evaluate` + 原生 `InputEvent`/`KeyboardEvent` 操作React表单。
比Playwright的 `type()`/`click()` 更可靠。被重定向时自动重试一次。
已在京东页面时有搜索框就直接搜，不跳回首页（更像真人）。

## 三、项目结构

```
tools/sourcing.js        ← 唯一对外的MCP工具（All-in-One，多action）。改这里
lib/ai-controller.js     ← 浏览器自动化核心（京东+淘宝的搜索/提取/详情/截图）
lib/chrome.js            ← Chrome会话管理（连本机正式Chrome，按profile管进程）
lib/accounts.js          ← 账号管理 + 登录态探测(probeAccountLoginStatus)
lib/strategy-engine.js   ← 策略引擎（买手店/销量/价格/利润率筛选规则）
lib/unit-price.js        ← 最小规格单价计算（每粒/每克/每毫升）
lib/sourcing-db.js       ← 数据库表结构 + 导出CSV
lib/db.js                ← 数据库封装
mcp/                     ← MCP服务器（server.mjs本地 / http-server.mjs多用户）
tests/                   ← 临时验证脚本（脏，不用管，别提交）
```

**注意**：tools/ 和 lib/ 下有很多旧文件（ai-select.js/full-selection.js等），
是早期版本遗留。**当前只用 sourcing.js 这一个 MCP 工具**，其他是历史包袱，
确认没引用可以删，但别急着动。
## 四、怎么用（MCP action 清单）

工具名 `ecommerce_sourcing`，靠 `action` 参数区分功能：

| action | 用途 | 关键参数 |
|---|---|---|
| `account_list` | 列出账号 | - |
| `account_login` | 扫码登录账号（人工扫码） | `accountId` |
| `account_check` | 探测账号真实登录态 | `accountId` |
| `jd_harvest` | **京东选品**：搜品牌→进买手店→扒商品→评价>2→截图 | `brand`, `maxPages`, `maxDetail` |
| `taobao_harvest` | **淘宝比价**：搜关键词→筛国内/48h/已售→进详情→截图 | `keyword`, `minSales`, `requireDomestic`, `require48h` |
| `export_results` | 导出选品结果为CSV | `outputPath`(可选) |
| `close` | 关闭浏览器 | `platform` |

**典型调用流程**（你作为调用方Agent要串起来）：
```js
// 1. 京东选品
jd_harvest({ brand:"swisse", maxPages:1, maxDetail:2 })
//   → 返回 candidates[]（每个含 title/price/comments/screenshotPath/shopType）
//   → 返回 agentInstructions（清洗指令，告诉你怎么过滤）

// 2. 对每个京东品，用"品牌+品名"去淘宝比价
taobao_harvest({ keyword:"Swisse PLUS 护肝", minSales:10 })
//   → 返回 candidates[]（含 shipFrom/isDomestic/ship48h/skuOptions/rejectReason）
//   → rejectReason 不为空的是该剔除的（如港澳台发货）

// 3. 你来清洗+配对+算利润，然后存库（调 db.saveSourcing）
// 4. export_results 导出CSV
```

**设计哲学**：脚本只负责"拉脏数据 + 截图"，**清洗/配对/判断交给调用方Agent**。
因为页面信息杂、规则会变，AI判断比死正则灵活。harvest 返回的 `agentInstructions`
字段就是给你的清洗指引。

## 五、账号现状（2026-06-10）

| 账号 | 平台 | 状态 |
|---|---|---|
| 京东账号一 | jd | ❌ 掉登录，需重新扫码 |
| 京东账号二 | jd | ✅ 已登录可用 |
| 淘宝账号一 | taobao | ✅ 已登录可用 |

- 登录预检会**自动跳过掉登录的账号**，用第一个已登录的，所以账号一掉了不影响跑。
- 但建议补登账号一，两个京东账号轮用，降低单账号风控风险。
- 补登方式：`account_login({ accountId:"<id>" })`，会开浏览器让**真人扫码**（AI替不了）。

## 六、还剩什么没做（按优先级）

### 1. 同款比对逻辑（最重要）
现在京东品和淘宝货源没有自动配对。需比对标题/SKU/功效关键词确认是同一款才配对算利润。
**怎么做**：进详情后比对 SKU规格/容量/功效关键词，至少保证"同规格同功效"才配对。

### 2. ~~saveSourcing 的 bestMatch 必须带 productId~~（已修复 ✓）

### 3. ~~策略引擎与 harvest 脱节~~（已修复 ✓）
筛选条件已从策略引擎读取，改策略文件即生效。

### 4. ~~CSV缺截图列~~（已修复 ✓）
导出增加京东截图、淘宝截图两列。

### 5. ~~浏览器反复关闭~~（已修复 ✓）
全部去掉了杀进程逻辑，浏览器打开后永不关闭。

### 6. ~~淘宝详情标题/店铺名抓错~~（已修复 ✓）
标题用 ItemHead + document.title 兜底，店铺名用 shopName 类只取第一行。

### 7. 京东搜索重定向问题（临时方案）
用 JS 原生事件 + 重试机制绕过，但账号被风控时仍会失败。终极方案需配合账号轮换+降频。
目前两个京东账号都因测试太频繁被风控，需冷却或手动过验证。

### 8. 批量选品规模化（目标一天500个品）
多品牌词轮询、多页翻页、账号轮换、失败重试、进度落库。
## 七、踩过的坑（务必注意，别重犯）

1. **京东搜索别只构造URL**。直接 `goto(search.jd.com?keyword=)` 会被反爬重定向回首页。
   必须模拟人类：开首页→鼠标移到搜索框→点击→打字→鼠标移到搜索按钮→点击。已实现。

2. **别全文搜地名判发货地**。淘宝标题有"澳洲进口"、店名可能带"香港"、
   "问大家"里有人问"是香港直邮吗"——全文搜会误判。
   只认快递行"X地 至 Y地"格式取 X。**产地≠发货地**（产地可以是澳洲，发货地是国内广东）。

3. **改页面解析前先看截图**。截图都存在 `<dataDir>/shots/jd|taobao/*.png`。
   别凭空猜DOM结构和正则——我吃过亏，发货地正则猜了两版都错，看了截图一次就对。

4. **浏览器一定要关干净**。连的是本机正式Chrome，CDP的Browser.close对persistent
   profile不退进程。已用 `waitForChromeProfileExit` 按profile精确杀进程兜底。
   跑完用 try/finally 调 `closeAiSessionsByPlatform` 确保关闭，别让浏览器常驻后台。

5. **工作前先验登录**。用 `openSessionChecked`（sourcing.js里），它会探测登录态、
   自动跳过掉登录的账号。别跳过这步直接干活，否则拿不到数据还不报错。

## 八、跑测试 / 验证

```bash
# 真实跑京东选品（会开浏览器，需账号已登录）
node -e "import('./tools/sourcing.js').then(async m=>{
  const {openSourcingDb}=await import('./lib/db.js');
  const ctx={dataDir:'/tmp/test'}; const db=openSourcingDb(ctx);
  const r=await m.handler(ctx,db,{action:'jd_harvest',brand:'swisse',maxPages:1,maxDetail:2});
  console.log(JSON.stringify(r,null,2));
})"
```
跑完检查：① candidates有数据 ② shots目录有截图 ③ `pgrep -f browser-profiles` 无残留进程。

## 九、和用户协作的方式

- **用户不懂编程**，用大白话解释，别堆术语。
- 用户是做电商生意的，关心"能不能稳定出可用的品"，不关心技术细节。
- 用户节奏快、要结果。遇到问题直接说现状+方案，别绕。
- 改完代码要**真跑验证**，别只说"应该能行"。用户被"看起来好了其实没好"坑过。
- 提交代码格式：`type: 描述`（feat/fix/refactor等），不要加AI署名。
- 只提交核心文件（tools/lib/mcp），tests/下的临时验证脚本别提交。

## 十、Git / 仓库

- GitHub: https://github.com/13739777296-del/ecommerce-sourcing-mcp-ai
- 最新稳定提交: `0944b6d`（修复登录预检/浏览器残留/发货地误判/导出action）
- 服务器部署（多用户）说明见 README.md

